import { Box, Typography } from "@mui/material";

export default function Home() {
    return (
        <Box>
            <Typography variant="h4" fontWeight={700}>Home Page</Typography>
        </Box>
    );
}
